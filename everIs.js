;(function(){
    'use strict'
    var EverIs = {

  
    }

    // 把 batNav 暴露给全局对象
    var _global = (function(){return this || (0,eval)('this');}());
    ('EverIs' in _global) || (_global.EverIs = EverIs);
}())